<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <header class="header">
        <a href="/admin">Admin</a>
        <a class="home-link" href="/">Home</a>
        <form action="/logout?_method=DELETE" method="POST">
            <button type="submit">Log Out</button>
        </form>
    </header>
    <form class="product-form" action="/admin/contact" method="POST">
        <div class="form-control">
            <label for="quote">Quote</label>
            <textarea type="text" name="quote" id="quote"><%= contacts.quote %></textarea>               
        </div>
        <div class="form-control">
            <label for="location">Location</label>
            <input type="text" name="location" id="location" value="<%= contacts.location %>">                
        </div>
        <div class="form-control">
            <label for="email">Email</label>
            <input type="text" name="email" id="email" value="<%= contacts.email %>">                
        </div>
        <div class="form-control">
            <label for="phone">Phone</label>
            <input type="text" name="phone" id="phone" value="<%= contacts.phone %>">                
        </div>
        <div class="button-control">
            <button class="btn" type="submit">Update Contacts Section</button>
        </div>
    </form>
</body>
</html>