<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <header class="header">
        <a href="/admin">Admin</a>
        <a href="/admin/add-work">Add Work</a>
        <a class="home-link" href="/">Home</a>
        <form action="/logout?_method=DELETE" method="POST">
            <button type="submit">Log Out</button>
        </form>
    </header>
    <% if (wrks.length > 0) { %>
        <div class="grid">
            <% for (let wrk of wrks) { %>
            <article class="card edu-card">
                <div class="card__order">
                    <h2><%= wrk.order %></h2>
                </div>
                <div class="card__school">
                    <b><%= wrk.school %></b>
                    <h5><%= wrk.date %></h5>
                </div>
                <div class="card__degree">
                    <b><%= wrk.degree %></b>
                    <h5><%= wrk.description %></h5>
                </div>
                <div class="card__actions">
                    <a href="/admin/edit-work/<%= wrk.id %>?edit=true" class="btn">Edit</a>
                    <br><br/>
                    <form action="/admin/delete-work" method="POST">
                        <input type="hidden" value="<%= wrk.id %>" name="workId">
                        <button class="btn" type="submit">Delete</button>                    
                    </form>
                </div>
            </article>
            <% } %>
        </div>
    <% } else { %>
        <h1>No Work Found!</h1>
    <% } %>
</body>
</html>